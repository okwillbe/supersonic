# Headless 语义层模块

该模块实现了 **Headless BI** 的核心功能，专注于构建和管理统一的语义数据模型。

## 子模块说明

* **`api`**: 定义语义层的对外标准接口（API），允许下游应用访问语义数据。
* **`core`**: 语义层的核心引擎，负责：
  * 元数据管理（模型、指标、维度、实体）。
  * 语义图谱构建。
  * SQL 生成逻辑。
* **`server`**: 语义层服务实现，处理 API 请求并协调核心组件。
* **`chat`**: 提供与语义层紧密结合的对话辅助功能（如基于语义元数据的问答增强）。
